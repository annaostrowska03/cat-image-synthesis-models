data:
  cats_dir: data/cats
  image_size: 64
  batch_size: 64
  num_workers: 0

model:
  z_dim: 128
  g_channels: 64
  d_channels: 64
  image_channels: 3

training:
  epochs: 50
  lr_g: 1.0e-4                 # ABLATION: lower learning rate
  lr_d: 1.0e-4
  beta1: 0.5
  beta2: 0.999
  label_smooth_real: 0.9
  use_gradient_penalty: true
  gp_lambda: 10.0
  n_critic: 5

logging:
  output_dir: outputs/ablation/dcgan_lr1e4
  checkpoint_dir: checkpoints/ablation/dcgan_lr1e4
  save_every_epochs: 50
  sample_size: 64

seed: 42
